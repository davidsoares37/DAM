package com.example.lifecyclemonitor

import android.widget.Button

class ThirdActivity : BaseActivity() {

    override val activityName: String = "ThirdActivity"
    override val activityColorRes: Int = R.color.activity_third_bg

    // Uses the default completion action (finish) from BaseActivity
}
